import { basePlotsUrl } from '../../util'
import { getData, getMinimalData } from '.'

export const minimalPlotsShowFixture = getMinimalData(basePlotsUrl)
export const plotsShowFixture = getData(basePlotsUrl)
